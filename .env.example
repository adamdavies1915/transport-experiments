# Database connection (for standalone deployment)
DATABASE_URL=postgresql://transit:transitpass@postgres:5432/nola_transit

# SSE endpoint
SSE_URL=https://nolatransit.fly.dev/sse

# Batch size for inserts (1 = insert immediately, higher = buffer before insert)
BATCH_SIZE=1

# Reconnect delay in milliseconds
RECONNECT_DELAY=5000

# PostgreSQL settings (for docker-compose)
POSTGRES_USER=transit
POSTGRES_PASSWORD=transitpass
POSTGRES_DB=nola_transit
